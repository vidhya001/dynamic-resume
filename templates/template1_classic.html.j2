<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{ profile.name }} - Resume</title>
  <style>
    body { font-family: Arial, sans-serif; font-size: 11pt; margin: 36px 54px; color: #222; }
    .name { text-align: center; font-size: 20pt; font-weight: bold; letter-spacing: 1px; }
    .contact { text-align: center; font-size: 10pt; margin-top: 4px; margin-bottom: 18px; }
    .section-title { background-color: #333; color: #fff; font-weight: bold; padding: 4px 6px; margin-top: 16px; margin-bottom: 6px; text-transform: uppercase; font-size: 10pt; }
    .job-header { font-weight: bold; }
    ul { margin-top: 2px; margin-bottom: 6px; }
  </style>
</head>
<body>
  <div class="name">{{ profile.name }}</div>
  <div class="contact">
    {{ profile.location }} · {{ profile.phone }} · {{ profile.email }}
  </div>

  <div class="section-title">Summary</div>
  <p>{{ summary }}</p>

  <div class="section-title">Professional Experience</div>
  {% for exp in experience %}
    <p class="job-header">
      {{ exp.company }}, {{ exp.location }} — <em>{{ exp.title }}</em>
      <span style="float:right;">{{ exp.start }} – {{ exp.end }}</span>
    </p>
    <ul>
      {% for b in exp.bullets %}
        <li>{{ b }}</li>
      {% endfor %}
    </ul>
  {% endfor %}

  <div class="section-title">Education</div>
  {% for edu in education %}
    <p class="job-header">
      {{ edu.degree }}, {{ edu.institution }} — {{ edu.location }}
      <span style="float:right;">{{ edu.date }}</span>
    </p>
    <ul>
      {% for d in edu.details %}
        <li>{{ d }}</li>
      {% endfor %}
    </ul>
  {% endfor %}

  <div class="section-title">Skills &amp; Other</div>
  <p><strong>Skills:</strong> {{ skills | join(', ') }}</p>
  {% if extra %}
  <ul>
    {% for item in extra %}
      <li>{{ item }}</li>
    {% endfor %}
  </ul>
  {% endif %}
</body>
</html>
